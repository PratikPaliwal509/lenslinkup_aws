[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "cd packages/db && npx prisma generate --config prisma.config.ts",
  "cd apps/api && npm run build"
]

[start]
cmd = "node apps/api/dist/server.js"
