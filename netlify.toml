[build]
  base    = "apps/web"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js requires the Netlify plugin for SSR / rewrites to work
[[plugins]]
  package = "@netlify/plugin-nextjs"
